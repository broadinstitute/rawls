package org.broadinstitute.dsde.rawls.dataaccess.drs

import scala.language.postfixOps

class DrsHubResolverSpec {
  // This class was created when we replaced MarthaResolverSpec.  In looking at those tests, they weren't really testing
  // anything valuable, so the tests have been removed.
  // DrsHubResolver similarly doesn't do much that is testable here, thus there are not any tests.
}
