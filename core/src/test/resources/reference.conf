# You'll need a MySQL and an OpenDJ if you're running this locally. Here are some:
# docker run --name rawls_test_mysql -e MYSQL_ROOT_HOST='%' -e MYSQL_ROOT_PASSWORD=rawls-test -e MYSQL_USER=rawls-test -e MYSQL_PASSWORD=rawls-test -e MYSQL_DATABASE=testdb -d -p 3306:3306 mysql/mysql-server:5.6
# docker run --name rawls_test_opendj -e ROOTPASS="testtesttest" -e BASE_DN=dc=dsde-dev,dc=broadinstitute,dc=org -v $(pwd)/docker/opendjsetup.sh:/opt/opendj/bootstrap/setup.sh -v $(pwd)/docker/example-v1.json:/opt/example-v1.json -d -p 389:389 broadinstitute/openam:opendj
# docker restart rawls_test_opendj

akka {
  loglevel = OFF
  logger-startup-timeout = 20s
}

submission-monitor-dispatcher {
  # Dispatcher is the name of the event-based dispatcher
  type = Dispatcher
  # What kind of ExecutionService to use
  executor = "fork-join-executor"
  # Configuration for the fork join pool
  fork-join-executor {
    # Min number of threads to cap factor-based parallelism number to
    parallelism-min = 2
    # Parallelism (threads) ... ceil(available processors * factor)
    parallelism-factor = 2.0
    # Max number of threads to cap factor-based parallelism number to
    parallelism-max = 20
  }
  # Throughput defines the maximum number of messages to be
  # processed per actor before the thread jumps to the next actor.
  # Set to 1 for as fair as possible.
  throughput = 1
}

submissionmonitor {
  trackDetailedSubmissionMetrics = true
}

integration.runFullLoadTest = false

backRawls = true

mysql {
  driver = "slick.driver.MySQLDriver$"
  batchSize = 5000
  host = "localhost"
  host = ${?MYSQL_HOST}
  port = 3306
  port = ${?MYSQL_PORT}
  db {
    url = "jdbc:mysql://"${mysql.host}":"${mysql.port}"/testdb?createDatabaseIfNotExist=true&useSSL=false&rewriteBatchedStatements=true"
    driver = com.mysql.jdbc.Driver
    user = "rawls-test"
    password = "rawls-test"
    connectionTimeout = "5 seconds"  // default of 1 sec sometimes too short for docker local mysql
  }
}

liquibase {
  changelog = "org/broadinstitute/dsde/rawls/liquibase/changelog.xml"
}

gcs {
  bucketLogsMaxAge = "180"
}

directory {
  url = "ldap://localhost:389"
  url = ${?DIRECTORY_URL}
  user = "cn=Directory Manager"
  user = ${?DIRECTORY_USER}
  password = "testtesttest"
  password = ${?DIRECTORY_PASSWORD}
  baseDn="dc=dsde-dev,dc=broadinstitute,dc=org"
  baseDn = ${?DIRECTORY_BASEDN}
}
